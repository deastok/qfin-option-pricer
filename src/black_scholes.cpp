#include "option_pricing/black_scholes.hpp"

namespace option_pricing {

    double black_scholes_call(
        double spot,
        double strike,
        double rate,
        double volatility,
        double time_to_expiry
        ) {
            return 0.0; // TODO: implement in v0.1
        }

    double black_scholes_put(
        double spot,
        double strike,
        double rate,
        double volatility,
        double time_to_expiry
    ) {
        return 0.0; // TODO: implement in v0.1
    }

}